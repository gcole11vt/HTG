import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("HTG")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

#Preview {
    ContentView()
}
